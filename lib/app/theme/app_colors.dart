import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primaryLight = Color(0xFF8B5CF6);
  static const primaryDark = Color(0xFF6B35E8);

  // Background Colors
  static const backgroundLight = Colors.white;
  static const backgroundDark = Color(0xFF1A1A1A);

  // Surface Colors
  static const surfaceLight = Colors.white;
  static const surfaceDark = Color(0xFF2A2A2A);

  // Text Colors
  static const textLight = Color(0xFF1A1A1A);
  static const textDark = Colors.white;

  // Secondary Colors
  static const secondaryLight = Color(0xFF64748B);
  static const secondaryDark = Color(0xFF94A3B8);

  // Error Colors
  static const errorLight = Color(0xFFDC2626);
  static const errorDark = Color(0xFFF87171);
}
